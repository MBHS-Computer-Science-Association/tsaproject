var assert = require('chai').assert;

var application = require('../index.js');

describe('application', function() {
  describe('users', function() {
    it('should manage the users properly');
  });
});
